import reverseObject from './reverseObject'

const inputValue = {
  hired: {
    be: {
      to: {
        deserve: 'I',
      },
    },
  },
}

const outputValue = reverseObject(inputValue)

console.log(outputValue)
